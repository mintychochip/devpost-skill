[project]
name = "devpost-mcp"
version = "0.1.0"
description = "MCP server for Devpost hackathons"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
    "beautifulsoup4>=4.12.0",
    "playwright>=1.40.0",
]

[project.scripts]
devpost-mcp = "devpost_mcp.server:main"
